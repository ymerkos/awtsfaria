/**
 * B"H
 */

export default /*css*/(shadowWidth)=>`
calc(
    -1 * ${shadowWidth}
) 
calc(
    -1 * ${shadowWidth}
) 0 #000,  
${shadowWidth} 
calc(
    -1 * ${shadowWidth}
) 0 #000,
calc(
    -1 * ${shadowWidth}
) ${shadowWidth} 
0 #000,
${shadowWidth} 
${shadowWidth} 
0 #000
`