/**
 * B"H
 */

export default /*css*/`
calc(
    -1 * var(--shadowWidth)
) 
calc(
    -1 * var(--shadowWidth)
) 0 #000,  
var(--shadowWidth) 
calc(
    -1 * var(--shadowWidth)
) 0 #000,
calc(
    -1 * var(--shadowWidth)
) var(--shadowWidth) 
0 #000,
var(--shadowWidth) 
var(--shadowWidth) 
0 #000
`