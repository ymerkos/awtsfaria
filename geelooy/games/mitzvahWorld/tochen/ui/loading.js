/**
 * B"H
 * The laoding screen to 
 * display while the game components
 * are loading.
 */

export default {
    shaym: "loading",
    className: "hidden loading",
    children: [
        {
            tag:"h2",
            innerHTML: "Loading"
        }
    ]
}